--- camel/camel-db.c.orig	2012-11-23 11:34:47.000000000 +0100
+++ camel/camel-db.c	2012-11-23 11:41:17.000000000 +0100
@@ -126,11 +126,9 @@
 
 	data = g_new0 (struct SyncRequestData, 1);
 	data->cFile = cFile;
-	data->flags = cFile->flags;
+	data->flags = cFile->flags = SQLITE_SYNC_NORMAL;
 	data->done = done;
 
-	cFile->flags = 0;
-
 	g_static_rec_mutex_unlock (&cFile->sync_mutex);
 
 	g_thread_pool_push (sync_pool, data, &error);
