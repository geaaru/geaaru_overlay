gitea:
  generator: github-1
  packages:
    - gitea:
        github:
          user: go-gitea
          repo: gitea
          query: releases
          tarball: gitea-src-{version}.tar.gz
        extensions:
          - golang
