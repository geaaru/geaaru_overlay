apache:
  generator: dirlisting-1
  packages:
    - apache:
        cat: www-servers
        dir:
          url: https://archive.apache.org/dist/httpd/
          format: tar.bz2
          order: asc
          files:
            - httpd
