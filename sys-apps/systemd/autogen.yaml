systemd:
  generator: github-1
  packages:
    - systemd:
        github:
          user: systemd
          repo: systemd-stable
          query: tags
          select: '^v251.*'
        versions:
          latest:
            template: systemd.tmpl
              #          249.12:
              #template: systemd.tmpl


